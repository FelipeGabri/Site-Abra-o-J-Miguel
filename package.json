{
  "name": "backend-noticias-agro",
  "version": "1.0.0",
  "description": "Servidor proxy para buscar notícias do agronegócio via NewsAPI, sem expor a chave da API no front-end.",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
